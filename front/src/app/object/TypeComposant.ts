export class TypeComposant {
  nom: string;
  titre: string;
  description: string;
  url: string;


  constructor(nom: string, titre: string, description: string, url: string) {
    this.nom = nom;
    this.titre = titre;
    this.description = description;
    this.url = url;
  }
}
